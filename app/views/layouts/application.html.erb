<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QAR</title>

  <!-- Meta Tags para CSRF y turbolinks -->
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  <!-- Bootstrap 4 y Bootstrap Icons -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css", integrity: "sha384-oP9cG7QsAT26eE2FasJ3h4wdCmLeCU+0PT9HiHj5/FEYX1+2O4E9Y58++kI4yFeX", crossorigin: "anonymous" %>
  
  <style>
    body {
      overflow-x: hidden;
    }

    .sidebar {
      width: 250px;
      height: 100vh;
      position: fixed;
      top: 0;
      left: 0;
      bottom: 0;
      transition: all 0.3s ease;
    }

    .sidebar.collapsed {
      width: 0;
      padding: 0;
      visibility: hidden;
    }

    .sidebar-footer {
      position: absolute;
      bottom: 10px;
      width: 100%;
      left: 0;
    }

    .content {
      margin-left: 250px;
      transition: margin-left 0.3s ease;
    }

    .content.collapsed {
      margin-left: 0;
    }

    /* Styles for toggle button */
    .sidebar-toggle {
      position: fixed;
      top: 15px;
      left: 15px;
      z-index: 1050;
      cursor: pointer;
    }
  </style>
</head>

<body class="bg-light">

  <% if user_signed_in? %>
    <div class="d-flex">
      <!-- Sidebar -->
      <aside class="sidebar bg-dark text-white p-4" id="sidebar">
        <h3 class="text-center mb-4">QAR</h3>
        <ul class="list-unstyled">
          <% if current_user.admin? %>
            <li><%= link_to 'Gestión de Usuarios', admin_users_path, class: 'text-white d-block py-2' %></li>
          <% else %>
            <li><%= link_to 'Mis datos', admin_users_path, class: 'text-white d-block py-2' %></li>
          <% end %>
          <li><%= link_to 'Empresas', companies_path, class: 'text-white d-block py-2' %></li>
          <li class="d-inline-block">
            <%= link_to 'Delegaciones', delegations_path, class: 'text-white d-inline-block py-2' %>
            <span class="badge badge-info ml-2"><%= Delegation.with_email(current_user.email).count %></span>
          </li>
          <li><%= link_to 'QRs', qrs_path, class: 'text-white d-block py-2' %></li>
          <li><%= link_to 'Declaraciones de Conformidad', reglamento_tecnicos_path, class: 'text-white d-block py-2' %></li>
          <% if current_user.admin? %>
            <li><%= link_to 'Reglamentos Técnicos', reglamento_tecnicos_path, class: 'text-white d-block py-2' %></li>
            <li><%= link_to 'Tipos de Procedimiento', tipo_procedimientos_path, class: 'text-white d-block py-2' %></li>
          <% end %>
        </ul>

        <!-- Sidebar Footer -->
        <div class="sidebar-footer mt-5 text-center w-90">
          <small>Bienvenido, <%= current_user.name %></small>
          <%= link_to 'Cerrar sesión', destroy_user_session_path, method: :delete, class: 'btn btn-danger btn-sm' %>
        </div>
      </aside>

      <!-- Content -->
      <main class="content flex-grow-1 p-4" id="main-content">
        <%= yield %>
      </main>

      <!-- Sidebar Toggle Button -->
      <button class="sidebar-toggle btn btn-dark" id="sidebar-toggle">
        <i class="bi bi-list"></i> <!-- Icono de menú hamburguesa -->
      </button>
    </div>
  <% else %>
    <div class="content mx-auto">
      <%= yield %>
    </div>
  <% end %>

  <!-- Scripts de Bootstrap y JavaScript personalizado -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
  
  <% if user_signed_in? %>
    <script>
      document.getElementById("sidebar-toggle").addEventListener("click", function() {
        document.getElementById("sidebar").classList.toggle("collapsed");
        document.getElementById("main-content").classList.toggle("collapsed");
      });
    </script>
  <% end %>
</body>
</html>