<%= form_with(model: qr) do |form| %>
  <% if qr.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(qr.errors.count, "error") %> prohibited this qr from being saved:</h2>

      <ul>
        <% qr.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :url_destino, style: "display: block" %>
    <%= form.text_field :url_destino %>
  </div>

  <div>
    <%= form.label :estado, style: "display: block" %>
    <%= form.number_field :estado %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
