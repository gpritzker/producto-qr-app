<!DOCTYPE html>
<html>
<head>
  <title>Recuperación de Contraseña | ProductQRApp</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>

<div class="container mt-5">
  <h2 class="text-center">¿Olvidaste tu contraseña?</h2>
  
  <% if resource.errors.any? %>
    <div class="alert alert-danger">
      <ul>
        <% resource.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card mx-auto" style="max-width: 400px;">
    <div class="card-body">
      <p class="text-muted text-center">Ingresa tu correo electrónico para recibir instrucciones de restablecimiento de contraseña.</p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "form" }) do |f| %>
        <div class="form-group">
          <%= f.label :email, "Correo Electrónico" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "correo@ejemplo.com" %>
        </div>

        <div class="text-center">
          <%= f.submit "Enviar instrucciones", class: "btn btn-primary btn-block" %>
        </div>
      <% end %>

      <div class="mt-3 text-center">
        <%= link_to "Iniciar sesión", new_session_path(resource_name), class: "text-primary" %> |
        <%= link_to "Registrarse", new_registration_path(resource_name), class: "text-primary" %>
      </div>
    </div>
  </div>
</div>

</body>
</html>