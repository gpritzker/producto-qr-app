<%= form_with(model: tipo_procedimiento) do |form| %>
  <% if tipo_procedimiento.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_procedimiento.errors.count, "error") %> prohibited this tipo_procedimiento from being saved:</h2>

      <ul>
        <% tipo_procedimiento.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
